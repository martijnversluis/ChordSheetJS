const sections: [string, boolean][] = [
  // name, generate shorthand tag (eg. sog, eot)
  ['abc', false],
  ['grid', true],
  ['ly', false],
  ['tab', true],
  ['textblock', false],
];

export default sections;
